<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <property name="connection.driver_class">org.mariadb.jdbc.Driver</property>
        <property name="connection.url">jdbc:mariadb://localhost:3306/employees</property>
        <property name="dialect">org.hibernate.dialect.MariaDBDialect</property>
        <property name="connection.username">intellij</property>
        <property name="connection.password">intellij</property>
        <property name="connection.pool_size">3</property>
        <property name="current_session_context_class">thread</property>

        <!-- PROPIEDADES MUY ÚTILES -->
        <!-- SHOW_SQL: Para que hibernate muestre las consultas SQL en el log -->
        <property name="show_sql">true</property>
        <!-- FORMAT_SQL: Para que la de arriba, además lo muestre bonito -->
        <property name="format_sql">true</property>

        <!-- PROPIEDAD MUY IMPORTANTE -->
        <!-- HBM2DDL.AUTO: Automatically validates or exports schema DDL to the database when the SessionFactory is
        created. Con "UPDATE" lo que haga a través de código se actualizará en base de datos -->
        <property name="hbm2ddl.auto">update</property>
    </session-factory>
</hibernate-configuration>
